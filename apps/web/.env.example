# Server-side only. The browser never talks to Laravel directly and never
# holds a bearer token — the session cookie is httpOnly.
API_BASE_URL=http://localhost:8000/api/v1
SESSION_COOKIE_NAME=sp_session
